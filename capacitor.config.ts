import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.openorder.lanlab",
  appName: "OpenOrder LAN Lab",
  webDir: "dist",
};

export default config;
